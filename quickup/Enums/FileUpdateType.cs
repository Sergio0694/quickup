namespace quickup.Enums
{
    /// <summary>
    /// An <see langword="enum"/> that indicates the type of file update for a single operation
    /// </summary>
    public enum FileUpdateType
    {
        Add,
        Update,
        Remove,
        Failure
    }
}